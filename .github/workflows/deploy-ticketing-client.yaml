# kaitest1
name: deploy-ticketing-client

on:
  push:
    branches:
      - master
      - feature-*
#   paths:
#     - 'client/**'
    tags: 
      - 'v*.*.*'

jobs:
  build:
    uses: ./.github/workflows/build-master.yaml
    with:
      APP_NAME: ticketing-client
